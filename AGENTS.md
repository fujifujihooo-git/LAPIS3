# LAPIS3 AI Agents Rules (AGENTS.md)

本ドキュメントは、LAPIS3リポジトリで作業するすべてのAIエージェントが遵守すべき最高規約（憲法）です。
詳細な運用規定は [01-development-rules.md](file:///d:/Antigravity/LAPIS3/docs/governance/01-development-rules.md) を参照してください。

---

## 1. モード制御 (Operation Modes)
指定がない場合は `AUDIT` をデフォルトとし、各モードのアクセス制限を遵守します。
- `AUDIT` / `EMERGENCY`: 読み取り専用（書き込み永久禁止）
- `DEVELOPMENT`: ローカル/エミュレータのみ書き込み許可

## 2. 5大原則 (Core Rules)
1. **本番データ保護**: 本番リソースへの書き込み操作は永久に禁止します。
2. **本番検証は参照のみ**: 本番環境での検証は読み取り専用とし、書き込み検証は Emulator/Staging でのみ行います。
3. **自己授権の禁止**: エージェント自身に権限を付与する規約の自己改定や、新ルールの即時適用を禁止します。
4. **すべての状態変更は個別承認**: コミット、Push、デプロイは例外なく事前承認を必要とします。
5. **承認前の監査証跡提示**: 承認を求める前に、変更点・テスト結果・ロールバック方法を提示しなければなりません。

## 3. 承認キーワード（特定性の確保）
実行には、以下の形式によるユーザーの明示的な指示（対象/目的の明記）が必要です。
- コミット: `「コミット承認: <対象・目的>」`
- Git Push: `「Push承認: <ブランチ名/目的>」`
- デプロイ: `「Deploy承認: <環境名>」`

※曖昧な「はい」や、対象が特定できない承認ワードはすべて未承認とみなします。
※ 承認範囲に迷った場合は、安全側に倒して必ず作業を停止し、ユーザーに確認してください。

## 4. ブラウザ操作・ログイン前の必須手順

ブラウザを使った動作確認・検証を行う場合、**ログイン操作を試みる前に必ず以下を実行すること**。

### MANDATORY: ログイン前チェック（省略禁止）
1. **[`.agent/TEST_CREDENTIALS.md`](file:///d:/Antigravity/LAPIS3/.agent/TEST_CREDENTIALS.md) を必ず読む**
   - プライマリアカウント: `lapis-test@lapis.local` / `Lapis3_2026!`
   - エミュレータURL・ローカルサーバーURLを確認する
2. **エミュレータ起動確認**: `start_emulator.bat` が実行済みで `All emulators ready!` が出ていることを前提とする
3. **ログイン失敗した場合**: 同じ認証情報で3回以上連続して再試行しない。エミュレータ状態・サーバー状態を先に確認すること

> **このルールを守らずにログイン操作を繰り返すことは、quotaトークンの無駄消費であり規約違反です。**

