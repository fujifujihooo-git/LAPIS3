# LAPIS3 AI Agents Rules (AGENTS.md)

このドキュメントは、本リポジトリで作業するすべての AI コーディングエージェント（Antigravity 等）が遵守すべき永続ルールです。開発検証時に絶対に事故を起こさないための極めて重要な指示が含まれています。

## 1. ログイン検証・アカウントに関する絶対ルール

### 🚫 絶対禁止：実在メールアドレスの使用
以下のメールアドレスは実在するアカウントであるため、ローカル開発環境・テスト環境・本番環境を問わず、**ログイン試行に絶対に使用してはなりません**。
* **`nakamura@nakamuraj.com`**

### 🔒 テスト検証時の使用アカウント制限
ローカル検証環境（`localhost` / `127.0.0.1`）でログインテストを行う場合は、**必ず**以下の実在しないことが保証されているテストアカウントのみを使用してください。
* **`lapis-test@lapis.local`** (パスワード: `Lapis3_2026!`)

これ以外のアカウント（`@example.com` 等も含む）でのログイン試行は、テスト環境のガードロジックによってシステム的にエラー（ブロック）となります。ログインに失敗した場合でも、自動生成したアドレスや別のアドレスに切り替えて再試行しないでください。

### ⚙️ テスト環境（localhost）での認証挙動
* テスト環境では、外部メール送信（GAS API）や Firestore への OTP 保存（`otp_codes`）は完全にバイパスされます。
* 認証コードは定数化された固定値 **`123456`** が自動的に割り当てられます。
* OTP入力モーダルに `【テストモード】認証コード: 123456` と表示されますので、その値をそのまま入力して検証を完了させてください。

## 2. テストデータの管理と個人情報保護

* テスト用のシードデータ（`setup_users.json` や Firestoreデータなど）を追加・変更する際は、絶対に実在する個人情報、実在企業名、実在メールアドレス（特に `@nakamuraj.com` ドメイン）を含めないでください。
* テスト用のアカウントには `@lapis.local` または `@example.com` などのダミードメインを使用してください。

## 3. 変更範囲管理（1チケット1目的）と正確な検証報告

### 🛠️ 変更範囲の厳格な管理
* **「1チケット1目的（単一責任の原則）」**を徹底してください。
* 修正を依頼された対象以外のコード（「ついで修正」や「無関係なコード整理・リファクタリング」）は**絶対に同時に行わない**でください。
* 変更範囲が広がると、依存関係による不整合や想定外のランタイムエラーを引き起こす原因になります。もし他の問題（例: `Invalid Date` などのバグ）を発見した場合は、今回の作業に含めず、必ず別の課題/チケットとして切り離して起票・対応してください。

### 📝 正確な検証報告
* 検証報告を行う際、**「実際に確認した環境」**と**「確認していない環境」**を明確に区別し、曖昧な表現や憶測を排除してください。
* 例：「ローカルで保存を確認したため、本番でも保存できる」といった推測を交ぜた報告は禁止します。「ローカル環境（エミュレータ）にて保存成功を確認。本番環境は表示確認のみ（保存テストは未実施）」のように、事実のみを正確に記述してください。
