# LAPIS3 AI Agents Rules (AGENTS.md)

本ドキュメントは、LAPIS3リポジトリで作業するすべてのAIエージェントが遵守すべき最高規約（憲法）です。
詳細な規定は [01-development-rules.md](file:///d:/Antigravity/LAPIS3/docs/governance/01-development-rules.md) を参照してください。

---

## 1. モード制御 (Operation Modes)
エージェントは、タスク開始時に指定された（指定がない場合は `AUDIT`）以下の動作モードを適用します。

| モード | 用途 | 制限内容 |
| :--- | :--- | :--- |
| **`AUDIT`** (推奨・デフォルト) | 本番・ステージング等の検証 | 書き込み絶対禁止。デプロイ・コミット禁止。 |
| **`DEVELOPMENT`** | ローカル開発・エミュレータ検証 | エミュレータ書き込み許可。承認付きコミット・デプロイ可。 |
| **`EMERGENCY`** | 障害調査・ログ取得 | 読み取り制限緩和。書き込み絶対禁止。 |

---

## 2. 7大防衛原則と優先順位

ルールが競合・衝突する場合は、以下の優先度（高 → 低）に従って最高優先度のルールを適用します。

1. **本番Firestoreデータの更新禁止**
   - 本番環境に対する書き込み（`updateDoc`, `setDoc`等）は、いかなる場合も**永久禁止**とします。
2. **環境識別の義務化と本番前提ルール**
   - リソースアクセス前に接続環境を特定・報告すること。特定不能時は「本番環境」とみなします。
3. **破壊的操作の制限**
   - データの削除、初期化、ファイル削除等は、候補を報告し承認を得るまで実行禁止。
4. **必須承認ゲートと明確な指示要件**
   - Git操作や設定変更等は、曖昧な表現ではなく `"git commitして"` 等の直接的指示がある場合のみ許可。
5. **デプロイ禁止**
   - エージェントによるデプロイコマンドの自動実行は禁止。手動または個別指示時のみ。
6. **Git操作の制限**
   - 指示のないコミットやPushの作成は禁止。明示的な承認指示に基づき実行。
7. **スコープの厳格な保護**
   - 依頼外のファイル変更・「ついでリファクタリング」は一切行わず、提案に留めること。

---

## 3. 承認付き許可 (Authorized Operations)
以下のコマンドや操作は永久禁止ではなく、**「人間（ユーザー）からの明示的な指示の直後」**に限り許可されます。

* `git add`, `git commit`（有効な指示: `"git commitして"`）
* `git push`（有効な指示: `"git pushして"`）
* `firebase deploy`（有効な指示: `"firebase deployして"`）

曖昧な表現（「終わらせて」「反映して」等）はすべて未承認として扱います。
迷った場合は必ず停止し、質問してください。
