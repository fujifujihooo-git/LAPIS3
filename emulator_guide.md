# Firebase Local Emulator 運用・手順ガイド

本番環境（Sparkプラン）のクォータ制限を回避し、ローカル環境で安全かつ確実に開発・テストを継続するための「Firebase エミュレータ」の起動・終了・データ管理に関する手順書です。

---

## 1. エミュレータの起動手順（作業を始めるとき）

開発作業や動作確認を開始する際は、必ず以下の手順でエミュレータを立ち上げてください。

### 手順
1. **VSCode等のターミナルを開く**
   LAPIS3のプロジェクトフォルダ（`C:\Users\nakamura\OneDrive\デスクトップ\AntigravityAPP\LAPIS3` 等）がカレントディレクトリになっていることを確認します。
2. **起動コマンド（バッチファイル）を実行する**
   ターミナル上で以下のコマンドを入力し、`Enter` を押します。
   ```bash
   ./start_emulator.bat
   ```
   > ※ または、エクスプローラーから `start_emulator.bat` を直接ダブルクリックしても構いません。

### 正常起動の確認
ターミナルに以下のようなメッセージが表示されれば準備完了です。
```text
┌─────────────────────────────────────────────────────────────┐
│ ✔  All emulators ready! It is now safe to connect your app. │
│ i  View Emulator UI at http://127.0.0.1:4005/               │
└─────────────────────────────────────────────────────────────┘
```
**【重要】 エミュレータを起動したこのターミナル（黒い画面）は、絶対に「×」ボタンで閉じないでください。最小化して裏で動かしたままにしておきます。**

---

## 2. 開発用サーバーの起動（LAPIS3画面の表示）

エミュレータとは「データベースの代わり」です。画面（HTML/JS）を表示するためには、これまで通りローカルサーバーも動かす必要があります。

### 手順
1. **別のターミナル（タブ・ウィンドウ）を開く**
   エミュレータが動いているのとは別のターミナル枠を立ち上げます。
2. **http-serverの起動**
   ```bash
   npx http-server
   ```
3. **ブラウザでアクセス**
   表示されたURL（例： `http://127.0.0.1:8080/` ）をブラウザで開きます。

---

## 3. エミュレータの終了手順（作業を終えるとき）🔥重要🔥

エミュレータ内のデータ（テスト登録した案件や顧客、アカウント情報など）は、**正しい手順で終了しないと消去されてしまいます**。
次回も同じデータ環境から再開するためには、以下の「データ書き出し（エクスポート）終了」を必ず行なってください。

### 手順
1. **エミュレータが動いているターミナル画面を表示する**
   （`start_emulator.bat` を実行している画面です）
2. **キーボードで `Ctrl` + `C` を同時に押す**
   「バッチ ジョブを終了しますか (Y/N)?」と聞かれたら、`Y` を入力して `Enter` を押します。
3. **エクスポートのログを確認する**
   ターミナル上に以下のようなログが流れれば、保存成功です。
   ```text
   i  emulators: Shutting down emulators.
   i  firestore: Exporting data to: ./emulator_data
   i  auth: Exporting data to: ./emulator_data
   ```

これで、作業中に追加・変更したすべてのデータが `emulator_data` フォルダに安全に保存されました。次回 `./start_emulator.bat` を起動した際には、自動的にこのデータが読み込まれた状態から再開できます。

---

## 4. トラブルシューティング（よくある質問）

### Q. 「データが見つからない」「ログインできない」
起動時に `./start_emulator.bat` を使わずに `firebase emulators:start` だけで起動してしまうと、前回保存したデータが読み込まれず「空のデータベース」で立ち上がってしまいます。**必ず `./start_emulator.bat` を使用**してください。

### Q. 認証コードやパスワードリセットのURLを知りたい
エミュレータ環境では外部へ実際のメールは送信されません。
ブラウザで [http://127.0.0.1:4005/auth](http://127.0.0.1:4005/auth) （Emulator UI）にアクセスするか、エミュレータのターミナルログを見ることで、送信されたはずのメール内容や認証用URL（Action code URL）を確認できます。

### Q. データがおかしくなったので、一番最初の状態（空っぽ）に戻したい
プロジェクトフォルダ内にある `emulator_data` というフォルダを丸ごと削除（またはリネーム）してから、再度 `./start_emulator.bat` を起動してください。初期状態に戻ります。
（その後必要に応じて、再度 `node seed.js` を実行してテスト用スタッフアカウントを作成してください）
